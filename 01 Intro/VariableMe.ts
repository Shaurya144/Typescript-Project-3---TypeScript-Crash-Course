let greeting: string = "Hello Sha"; // this is how you should define variables
// It stops accidentally assigning different types to the same variable
console.log(greeting)

// Primitive types - sting, number, boolean
// Int and Floats are not types, you just use number

let UserId: number = 32452.35
let LoggedIn: boolean = true

export {}